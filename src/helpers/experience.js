const getLevel = s => s / 1;

const calcTNL = s => s / 1;

const levelUp = s => s / 1;

export default {
  getLevel,
  calcTNL,
  levelUp
};
/*
let baseExperience = 100;
let targetExp = baseExperience * skill.levelScale;
let level = 1;

while (exp > targetExp) {
  exp -= targetExp;
  level += 1;

  targetExp += level * baseExperience * skill.levelScale;
}
*/
